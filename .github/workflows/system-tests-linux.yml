# GitHub Actions workflow for Linux system tests
# Architecture: TeamCity server (Docker) + TeamCity agent (Docker with MATLAB)
# Note: Update the MATLAB version used in this workflow whenever a new release becomes available.
#
# Requirements:
#   - GitHub-hosted ubuntu-latest runner
#   - Repository secret: MLM_LICENSE_TOKEN (for MATLAB batch licensing)

name: System Tests (Linux)

on:
  pull_request:
    branches: [main]
    paths:
      - 'matlab-plugin-agent/**'
      - 'matlab-plugin-server/**'
      - 'matlab-plugin-common/**'
      - 'build/**'
      - 'pom.xml'
  push:
    tags: ['v*']
  workflow_dispatch:

jobs:
  system-test-linux:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Java (JDK 21)
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.14'

      - name: Install Python dependencies
        run: pip install requests

      - name: Build plugin
        run: mvn package -DskipTests -q

      - name: Start containers
        working-directory: systemtest/ci-linux
        env:
          MLM_LICENSE_TOKEN: ${{ secrets.MLM_LICENSE_TOKEN }}
          PLUGIN_ZIP: ${{ github.workspace }}/target/matlab-plugin.zip
        run: |
          docker compose build
          docker compose up -d

      - name: Setup TeamCity server and agent
        run: python -u setup_teamcity.py
        working-directory: systemtest/ci-linux

      - name: Configure project
        run: python -u configure_project.py
        working-directory: systemtest
        env:
          MATLAB_PATH: /opt/matlab

      - name: Run builds
        run: python -u run_builds.py
        working-directory: systemtest

      - name: Collect server logs
        if: always()
        run: |
          mkdir ci-logs
          docker cp teamcity-server:/opt/teamcity/logs/ ci-logs/server/ || true
          docker logs teamcity-server > ci-logs/server-stdout.log 2>&1 || true
          docker logs teamcity-agent > ci-logs/agent-stdout.log 2>&1 || true

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: system-test-linux-logs
          path: ci-logs/
          retention-days: 7

